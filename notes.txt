# of players = p
# of matches (round 1) = 1/2 * 2^ceiling(log2(p)) = Math.pow(2,Math.ceil(Math.log2(p)))/2 = m

players = [1player1,player2,...]
rounds = [round1,round2,...]
game = 	game.opponent1
		game.opponent2
		game.winner
		game.loser
round = [game1,game2,...]



TODO - lookup angular factories

kagany vaweerdy